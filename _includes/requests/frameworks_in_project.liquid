{%- comment -%}
List all frameworks of a project 

Arguments:
- project: the project to list frameworks from

Returns:
- result: a collection of frameworks
{%- endcomment -%}
{%- capture raw -%}
    {%- for language in include.project.language -%}
        {%- for language_fragment in language -%}
            {%- for framework in language_fragment[1].frameworks -%}
                {{ framework }},
            {%- endfor -%}
        {%- endfor -%}
    {%- endfor -%}
{%- endcapture -%}
{%- assign size = raw | size | minus: 1 -%}
{%- assign result = raw | slice: 0, size | split: ',' -%}