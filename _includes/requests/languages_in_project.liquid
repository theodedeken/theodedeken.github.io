{%- comment -%}
List all languages of a project 

Arguments:
- project: the project to list languages from

Returns:
- result: a collection of languages
{%- endcomment -%}
{%- capture raw -%}
    {%- for language in include.project.language -%}
        {%- for language_fragment in language -%}
            {{ language_fragment[0] }},
        {%- endfor -%}
    {%- endfor -%}
{%- endcapture -%}
{%- assign size = raw | size | minus: 1 -%}
{%- assign result = raw | slice: 0, size | split: ',' -%}