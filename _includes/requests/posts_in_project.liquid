{%- comment -%}
Lists all posts for a given project

Arguments:
- project: the given project key

Returns:
- result: a collection of post links
{%- endcomment -%}
{%- capture raw -%}
    {%- for post in site.posts -%}
        {%- if post.project == include.project -%}
            {{ post.permalink }},
        {%- endif -%}
    {%- endfor -%}
{%- endcapture -%}
{%- assign size = raw | size | minus: 1 -%}
{%- assign result = raw | slice: 0, size | split: ',' -%}
